# 经 nginx 同源访问。
#
# ★ 只能写到 origin，不能写成 http://fifa.techstz.com/atlas ——
#   http.ts 用 `new URL(path, API_BASE)` 拼地址，而路径以 "/" 开头时
#   会把 base 的路径部分整个丢掉（/atlas 会被吃掉）。所以反代要落在
#   根上的 /v1/，而不是某个前缀下。
#
# ★ 同源之后不再有 CORS；也不能再用 127.0.0.1:8000 —— 那是**浏览器所在
#   机器**的回环地址，不是服务器的。
NEXT_PUBLIC_API_BASE=http://fifa.techstz.com
